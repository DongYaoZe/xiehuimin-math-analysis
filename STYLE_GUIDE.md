# 统一转录与数学排版规范

本规范对所有 agent 强制生效。目标是“忠实转录 + 统一数学排版”，不是改写、摘要或二次创作。

## 1. 忠实性

1. 正文、标题、定理、例题、练习、证明、注释等按原书逐字转录；不得概括、润色或补写作者没有写的解释。
2. 省略扫描页的页眉、页脚和印刷页码；不要在成书正文加入“来源于第几页”“原书扫描件”等编辑说明。
3. 目录页不人工抄录，最终目录由 LaTeX 自动生成；正文中的章、节、小节层级按原书恢复。
4. 遇到疑似原书排印错误：不要凭感觉偷偷改。若能确定为排印错误，可修正 LaTeX，并在源码同行或下一行加 `% SOURCE-ERR: 原书作……`；若不确定，忠实保留并加 `% CHECK-SOURCE: ...`。
5. 看不清的字符、上下标、正负号、积分限等，不得猜测。先查看相邻页或放大原图；仍不能确认时写 `% CHECK-SOURCE`，不要编造。
6. 不做批量 OCR。以模型直接读页图为主；OCR 只能作为个别难字的最后辅助手段，且必须回看原图确认。

## 2. 中文与标点

1. 中文正文使用全角中文标点：`，。；：？！“”‘’（）`。
2. 中文与行内数学之间不手工插空格，例如 `当 $x>0$ 时`。
3. 中文文字出现在数学公式中时用 `\text{}`，例如 `x_{\text{临界}}`。严禁把中文放进 `\mathrm{}`、`\rm`、`\operatorname{}`。
4. 西文专名按原书保留，例如 Cauchy、Taylor、Fourier、Cantor、Stolz、Bernoulli、Gauss、Stokes。

## 3. 微分、常数与函数名

1. **微分算子一律正体**，统一用项目宏 `\dd`：
   - `\dd x`
   - `\int_a^b f(x)\,\dd x`
   - `\frac{\dd y}{\dd x}`
   - `\frac{\dd^n y}{\dd x^n}`
   - `\dd f=f'(x)\,\dd x`
   禁止直接写数学斜体 `dx`、`d x` 或 `$d$` 充当微分算子。
2. 偏导使用 `\partial`：`\frac{\partial f}{\partial x}`；混合偏导的变量顺序按原书。
3. Euler 常数底和虚数单位使用正体宏 `\ee`、`\ii`，例如 `\ee^x`、`\ee^{\ii t}`。
4. 标准函数/算子必须用 LaTeX 算子命令：`\sin`、`\cos`、`\tan`、`\cot`、`\ln`、`\log`、`\exp`、`\lim`、`\sup`、`\inf`、`\max`、`\min`、`\det`。其他多字母算子用 `\operatorname{...}` 或项目已有 `\DeclareMathOperator`，不得写成普通斜体字母串。
5. `\pi`、变量、参数、一般常数 $a,b,C,n$ 保持数学斜体，除非它们是文字性缩写或算子。
6. 渐近记号统一使用正体项目宏 `\bigO`、`\littleo`，例如 `f(x)=\bigO(x^2)`、`f(x)=\littleo(x)`；常值记号可用 `\const`。

## 4. 集合、向量和范数

1. 常用数集用 `\RR, \CC, \NN, \ZZ, \QQ`。
2. 绝对值和范数优先用 `\abs{...}`、`\norm{...}`；仅在确实需要自动伸缩时用星号形式 `\abs*{...}`、`\norm*{...}`。
3. 向量统一用 `\vect{x}`；矩阵在确实作为矩阵对象时用 `\mat{A}`。若原书把点写成普通坐标元组，不强行改成粗体。
4. 映射写作 `f\colon A\to B`，冒号用 `\colon`。
5. 集合描述优先写 `\{x\in A\mid P(x)\}`。

## 5. 公式结构

1. 行内短式用 `$...$`；重要独立公式用 `\[...\]`。
2. 原书印有公式编号时，使用 `equation`/`align` 并用 `\tag{原编号}` 保留原编号，例如：
   ```tex
   \begin{equation}
     a_n\to A.
     \tag{2.1}
     \label{eq:2.1}
   \end{equation}
   ```
   原书没有编号的显示公式不要自行编号。
3. 多行等式使用 `align` / `aligned` / `gathered`；禁止用连续空格、`\qquad` 硬把超长公式塞在一行。
4. 分段函数用 `cases`；矩阵按原书括号类型用 `pmatrix`、`bmatrix`、`vmatrix` 等。
5. 省略号按语义选择 `\ldots`、`\cdots`、`\vdots`、`\ddots`。
6. 简单括号不要滥用 `\left`/`\right`；只有内容较高时才自动伸缩。
7. 积分号前后不要人为堆空格；积分测度前用细空格：`\int f(x)\,\dd x`。

## 6. 章、节、例题与练习环境

1. 章标题已经预置在 `chapters/chXX.tex`，不要重复创建第二个 `\chapter`。
2. 原书 `§5.2` 等使用 `\section{...}`；更细层级使用 `\subsection{...}`。不要把普通粗体段落误升格成章节。
3. 定义、定理、引理、命题、例、练习、思考题、参考题分别使用项目环境：
   - `xdefinition`
   - `xtheorem`
   - `xlemma`
   - `xproposition`
   - `xexample`
   - `xexercise`
   - `xthought`
   - `xreferenceproblem`
4. 若原书有明确编号，放在可选参数中，例如 `\begin{xexample}[5.2.1]`；没有编号时不要虚构编号。
5. 原书的“解”“证明”“注”分别使用 `xsolution`、`xproof`、`xremark`。
6. 原书中的“内容提要”“学习要点”“对于教学的建议”等若只是结构性小标题，优先使用 `\subsection*{...}` 或 `\paragraph{...}`，按视觉层级判断，不新增模型自创标题。

## 7. 引用与标签

1. 同一文件内能够确定的公式、定理、例题可补 `\label` 并使用 `\eqref` / `\ref`。
2. 标签约定：`eq:章节.编号`、`sec:章节.节`、`exm:原编号`、`thm:原编号`。
3. 跨章节引用若当前无法确定 label，先忠实保留原书字面编号，不要为了“智能交叉引用”修改原文。

## 8. 图形

1. 原书有图时，只能从 `source-pages/` 对应页裁取。**禁止重新绘制、禁止调用生图模型、禁止凭想象补图。**
2. 裁图保存在 `figures/upper/chXX/` 或 `figures/lower/chXX/`。尽量只保留图形本身；图题/图号若在正文中可用 LaTeX 重排，就不要把大片正文一起截入图。
3. 允许的图像处理仅限：裁剪、旋转校正、灰度化、轻度黑白电平/对比度调整。不得改变数学几何关系、线条位置、标注内容。
4. 正文一般使用：
   ```tex
   \begin{center}
     \includegraphics[width=.55\linewidth]{figures/upper/ch05/fig-5-1.png}
   \end{center}
   ```
5. 不在图片下面增加“原书扫描图”“不再使用 TikZ”等编辑说明。

## 9. 禁止事项

- 不批量 OCR 后直接粘贴。
- 不改写作者文风，不把教材变成 AI 讲义。
- 不自行补充定理、证明、例题、总结。
- 不为了排版方便删除原书条件、脚注、公式编号。
- 不在章节文件中新增宏包、新命令或全局格式；需要新宏时在报告中提出，由整合者统一加到 `preamble/`。
- 不编译、不 push、不改 README/规范/其他章节，除非任务明确要求。

## 10. 每个 agent 的最低验收

- 指定扫描页全部看过，无明显漏段、漏公式、漏小题。
- 章节层级和原书一致。
- 微分 `\dd`、Euler `\ee`、虚数单位 `\ii`、标准函数名等符合本规范。
- 没有中文进入 `\mathrm`/`\rm`。
- 没有 TODO、`???`、伪造内容；无法确认处使用 `% CHECK-SOURCE`。
- 只修改任务指定的 `.tex` 和本章 `figures/...`。
